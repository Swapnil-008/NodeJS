//Initally javascript engine was in browsers only, so we weren't able to run the javascript outside the browers.
//Node.js -> v8 Engine + c++
//Node.js provides the environment on your machine, so that you could execute the javascript on your machine, javascript code executes in v8 engine which is inbuilt in c++.
//With that we can create webservers in javascript language.

//Node.js is a Runtime environment for javascript

//npm init -> to initialize the packate

// const math = require('./math')   // If inside require you give the module name directly without './' then it finds the package inside node, and if you mentioned any directory such as './' (current directory) then it finds in mentioned directory.

//There are two types of js -> 1) module js,  2) common js