console.log("Hello world!")

//We can't access the window object in node, but it is accessible on browser, because while adding the v8 engine (javascript engine) in node these things are removed (some other related to UI e.g. (alert())).

